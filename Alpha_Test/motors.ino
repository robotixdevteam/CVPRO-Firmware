// Test-Code for CVPRO
// for running the motors in Forward Direction

void motors() {
  lf(255, 0);
  lb(255, 0);
  rf(255, 0);
  rb(255, 0);

  // delay(1000);

  // lf(0, 255);
  // lb(0, 255);
  // rf(0, 255);
  // rb(0, 255);

  // delay(1000);
}